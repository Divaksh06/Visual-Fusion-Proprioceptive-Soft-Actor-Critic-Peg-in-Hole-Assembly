# NOTE: pyroki must be installed from source FIRST:
#   git clone https://github.com/chungmin99/pyroki.git
#   cd pyroki && pip install -e . && cd ..
# This also installs jax, jaxlib, jaxlie, jaxls, yourdfpy, jax-dataclasses.

mujoco>=3.0.0
gymnasium>=0.29.0
torch>=2.1.0
numpy>=1.24.0
pyyaml>=6.0
scipy>=1.11.0
wandb>=0.16.0
imageio>=2.31.0
